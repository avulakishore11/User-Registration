trigger:
- main

pool:
  name: 'userreg-pool'



stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - script: sudo apt-get update && sudo apt-get install openjdk-8-jdk -y
      displayName: 'Install OpenJDK 8'

    - task: Maven@3
      displayName: 'Build finished'
      inputs:
        mavenPomFile: 'pom.xml'
        mavenOptions: '-Xmx3072m'
        javaHomeOption: 'Path'
        jdkVersionOption: '1.8'
        jdkArchitectureOption: 'x64'
        publishJUnitResults: true
        testResultsFiles: '**/surefire-reports/TEST-*.xml'
        goals: 'clean package'
    - task: CopyFiles@2
      displayName: 'copy the build output'
      inputs:
        Contents: '**'
        TargetFolder: '$(Build.ArtifactStagingDirectory)'
